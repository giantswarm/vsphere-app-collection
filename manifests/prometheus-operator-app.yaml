apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: prometheus-operator-app
  namespace: argocd
spec:
  destination:
    namespace: monitoring
    server: https://kubernetes.default.svc
  project: collections
  source:
    path: .
    plugin:
      env:
      - name: KONFIGURE_APP_NAME
        value: prometheus-operator-app
      - name: KONFIGURE_APP_VERSION
        value: 0.8.2
      - name: KONFIGURE_APP_CATALOG
        value: control-plane-catalog
      name: konfigure
    repoURL: https://github.com/giantswarm/config.git
    targetRevision: FIXME
  syncPolicy:
    automated:
      allowEmpty: true
      prune: true
      selfHeal: true
